# Revisions git blame should look through rather than at.
#
# A commit that only reformats code makes `git blame` useless: every line points
# at the reformat instead of at the change that introduced it. Listing the commit
# here restores the real answer.
#
# Enable it once per clone (it is a local setting, not a repository one):
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# GitHub reads this file automatically in its blame view.
#
# Rules for adding a line
#   - The commit must change formatting and nothing else. If it also fixes a bug,
#     the bug fix becomes invisible to blame — split it first.
#   - Full 40-character SHA. Abbreviated hashes are rejected by git.
#   - One commit per line, with a comment saying what it reformatted.
#
# landing/: prettier + prettier-plugin-tailwindcss across app/, components/,
# i18n/, lib/ and the three configs. 23 files, quote style, wrapping and Tailwind
# utility order only.
5e000a5260509df39b683a166f34e9e8ba12c34a

# CONTRIBUTING.md, brand/README.md, docs/architecture/README.md: prettier on the
# three docs edited by the commit before it. Markdown table column padding, one
# *emphasis* to _emphasis_, one missing trailing blank line. proseWrap is
# preserve, so no prose moved.
7c6455428bc259112a013b74bd32b6d3cf766eab

# backend/src/routes/web.ts, docs/architecture/api.md, docs/qa/testing-strategy.md,
# ios/.swiftlint.yml: prettier on the four files the commit before it edited. web.ts
# is 750 lines of it and is the first slice of the backend/src debt below to be paid
# off; the rest is table padding and YAML comment alignment.
f976e92ad4b12d6f1c1162c7af21487c99da93b3

# PENDING: the rest of backend/src has not been formatted yet. 57 of its 90
# TypeScript files are still dirty against .prettierrc.json, and formatting them
# would move 12,870 lines (measured 2026-08-20 — the command is in CONTRIBUTING.md
# under "Formatting is a ratchet, not a wall"). CI enforces formatting only on the
# files a change touches (the `format` job in .github/workflows/ci.yml), so the
# formatted share grows instead of arriving in one commit nobody can review.
# When that reformat is finally run — `make format`, on its own branch, with no
# other change in it — add its SHA below.
