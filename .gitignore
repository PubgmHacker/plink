# ─── macOS ──────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
# Файл кастомной иконки папки в Finder называется "Icon\r" — ровно пять
# символов. Без якоря шаблон `Icon?` съедал и каталог docs/icons целиком,
# из-за чего направления иконки молча не попадали в репозиторий.
/Icon?
._*
.Spotlight-V100
.Trashes

# ─── Xcode (project is generated by XcodeGen — do NOT commit .xcodeproj) ─
*.xcodeproj/
*.xcworkspace/
xcuserdata/
*.xcuserstate
*.xccheckout
*.moved-aside
DerivedData/
build/
Build/
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# ─── Swift Package Manager ──────────────────────────────
.swiftpm/
Package.resolved
.build/

# ─── Node / backend ─────────────────────────────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnp
.pnp.js
dist/
coverage/
.nyc_output/
*.tsbuildinfo

# ─── Prisma ─────────────────────────────────────────────
prisma/migrations/dev.db*
*.db-journal

# ─── Secrets (NEVER commit) ─────────────────────────────
.env
.env.*
!.env.example
Secrets.xcconfig
GoogleService-Info.plist
*.p8
*.p12
AuthKey_*.p8

# ─── Logs / runtime ─────────────────────────────────────
*.log
logs/
.env.local

# локальные скретч-скрипты с ключами — никогда не коммитить
test.py
*.local.py

# локальный конфиг агента с ключами — никогда не коммитить
opencode.json
check.sh
